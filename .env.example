# Copy to .env (git-ignored) and fill in. Loaded by Makefiles via `-include`,
# so use plain KEY=value (no quotes, no `export`, no spaces around `=`).
ANTHROPIC_API_KEY=sk-ant-...
# Path to a raw Windows 95 HDD image, used by `make setup-win95`.
WIN95_IMG=
